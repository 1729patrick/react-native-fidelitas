import { Dimensions } from 'react-native';

const { height, width } = Dimensions.get('window');

export const CAMERA_HEIGHT = height * 0.7;
export const CAMERA_WIDTH = width;

export const SCANNER_SIZE = width * 0.6;
