import { StatusBar } from 'react-native';

export const STATUS_BAR_HEIGHT = StatusBar.currentHeight || 25;
export const HEADER_HEIGHT = 47;
export const TOTAL_HEADER_HEIGHT = HEADER_HEIGHT + STATUS_BAR_HEIGHT;
