import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttons: {
    width: '100%',
    marginTop: 'auto',
    paddingBottom: 35,
    paddingHorizontal: 20,
  },
  button: {
    marginTop: 16,
  },
});
